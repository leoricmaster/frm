# GitLab / Runner 运行时数据（容器挂载卷，不入库）
config/
logs/
data/
runner-config/

# Playwright 虚拟环境
.venv/

# 截图作为验证产物保留入库（screenshots/ 不忽略）

# MinIO 运行时数据（二阶段）
minio-data/

# Harbor 运行时数据（二阶段 §5.3）
harbor-data/

# Harbor prepare 生成的运行时配置（含随机密钥，每次 prepare 重生成）
harbor-installer/common/
harbor-installer/input/
